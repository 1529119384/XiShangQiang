<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="uno.acloud.mapper.UserMapper">

    <select id="findByUsernameAndPassword" resultType="User">
        select user_id as userId, username, password, position
        from user
        where username = #{username} and password = #{password}
    </select>

    <select id="findByUsername" resultType="User">
        select user_id as userId, username, password, position
        from user
        where username = #{username}
    </select>

    <select id="countByUsername" resultType="int">
        select count(*)
        from user
        where username = #{username}
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        insert into user (username, password, position, nickname)
        values (#{username}, #{password}, #{position}, #{nickname})
    </insert>

</mapper>
